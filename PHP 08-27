<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <link rel="stylesheet" href="{{ asset('assets/css/index.css') }}">
</head>
<body>
    <header class="row header">
        <h1>CRUD-DEMO</h1>
        <nav>
            <a href="#">Жагсаалт</a>
            <a href="/posts/create">Шинэ пост</a>
        </nav>
    </header>
    <h1>This is posts page</h1>
   @forelse ($posts as $item)
    <div class="card">
        <h3>{{ $item->title }}</h3>
        <div style="opacity: 0.7">{{ $item->created_at }}</div>
        <div> {{$item->content}} </div>
      <a class="btn" style='border: 10px soild green; color: green; border-radius: 16px'href="{{route('post.edit',['id'=> $item->id])}}">Edit post</a>
      <a class="btn" style='border: 10px soild red; color: red; border-radius: 16px'href="{{route('post.destroy',['id'=> $item->id])}}">Delete post</a>
      <a class="btn" style='border: 10px soild blue; color: blue; border-radius: 16px'href="{{route('post.show',['id'=> $item->id])}}">View details</a>
    </div>
   @empty
    <p>одоогоор пост алга</p>
   @endforelse


</body>
</html>
